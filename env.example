# Backend
# Put this in repo root .env (do NOT commit the real key)
FIRMS_MAP_KEY=your_firms_key_here

# Frontend calls the backend here (place in frontend/.env.local)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
